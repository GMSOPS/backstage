apiVersion: "argoproj.io/v1alpha1"
kind: "Application"
metadata:
  name: "kisobran-${{values.app}}-production"
  namespace: "argo-cd"
  finalizers:
  - "resources-finalizer.argocd.argoproj.io"
spec:
  destination:
    namespace: "argo-cd"
    server: "https://kubernetes.default.svc"
  project: "${{values.app}}-production"
  source:
    path: "./argocd/production"
    repoURL: "https://github.com/${{values.gitOwner}}/${{values.app}}-infrastructure.git"
    targetRevision: "main"
    helm:
      valueFiles:
      - values.yaml
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
    - "CreateNamespace=true"
