argo-apps:
  spec:
    destination:
      server: "${{values.productionApiServerUrl}}"
    project: "${{values.app}}-production"
    suffix: "production"

  apps:
    ${{values.app}}:
      ns: ${{values.app}}
      source:
        url: "https://github.com/${{values.gitOwner}}/${{values.app}}-infrastructure.git"
        branch: "main"
      path: ./crossplane/production
      autoSync: true


